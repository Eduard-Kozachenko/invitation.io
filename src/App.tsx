import React from "react";
import Envelope from "./components/Envelope";

const App: React.FC = () => {
  return (
    <React.Fragment>
      <Envelope />
    </React.Fragment>
  );
};

export default App;
